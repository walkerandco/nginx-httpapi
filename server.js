"use strict";

module.exports = {
  /**
   * [description]
   * @param  {[type]} a [description]
   * @param  {[type]} b [description]
   * @return {[type]}   [description]
   */
  testsum: function (a, b) {
    return a + b;
  }
  // test > testsum function
  // # testsum(1, 2) == 3 (returns the sum of both params)
  // # testsum(3, 4) == 7 (returns the sum of both params)

}
